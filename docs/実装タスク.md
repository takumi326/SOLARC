# 収支管理MVP 実装タスク (初版)

## 方針

- まずAPIを完成させ、次にUIを接続する
- 月次管理モデル (DB v3 / API v1 / UI v2) をMVPスコープとする
- 1PR 1目的で進める

## フェーズ1: 基盤セットアップ

- [ ] `api/` Rails APIプロジェクトを作成
- [x] `web/` React + TypeScript (Vite) — **廃止**（Rails HTML に移行済み）
- [ ] CORSと環境変数の雛形追加
- [ ] `docker-compose` でローカルPostgreSQLを起動可能にする

## フェーズ2: DBとモデル

- [ ] migrations作成: categories, payment_methods, expenses, incomes
- [ ] migrations作成: forecasts, actuals, expense_actuals, income_actuals, monthly_balances
- [ ] index/unique制約を設計書通りに反映
- [ ] Rails model validation (金額符号、参照整合)

## フェーズ3: API (マスタ系)

- [ ] `/api/categories` CRUD
- [ ] `/api/payment_methods` CRUD
- [ ] `/api/expenses` CRUD
- [ ] `/api/incomes` CRUD

## フェーズ4: API (業務系)

- [ ] `/api/monthly_balances` GET/PUT
- [ ] `/api/forecasts` GET/PATCH
- [ ] `/api/forecasts/bulk_update` POST
- [ ] `/api/actuals` GET/POST/PATCH/DELETE
- [ ] `/api/actuals/import` POST

## フェーズ5: API (集約)

- [ ] `/api/dashboard` GET 実装
- [ ] 月次予測計算ロジックをサービス化
- [ ] 異常系レスポンス形式統一

## フェーズ6: UI

- [ ] ダッシュボード (`/`)
- [ ] 実績入力モーダル
- [ ] マスタ設定 (`/masters`)
- [ ] インポート (`/import`)
- [ ] API接続とエラーハンドリング

## フェーズ7: 品質

- [ ] RSpec (request/model) 最低限追加
- [ ] Vitest + RTL で主要画面テスト
- [ ] E2E (任意、最小シナリオのみ)
- [ ] READMEに起動手順・ENV手順を記載

## 直近の着手順 (具体)

1. Railsプロジェクト生成 (`api/`)
2. 9テーブル分migration作成
3. categories APIのCRUDを最初に通す
4. 同パターンで残りマスタAPIを横展開
