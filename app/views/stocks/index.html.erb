<div class="space-y-4">
  <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <div class="mb-3 flex flex-wrap items-center justify-between gap-2">
      <h2 class="text-xl font-bold">株一覧</h2>
      <div class="flex flex-wrap items-center gap-2">
        <%= link_to "業種を管理", industries_path, class: btn_secondary_classes %>
        <%= link_to "銘柄取込", new_import_stocks_path, class: btn_secondary_classes %>
        <%= link_to "監視銘柄取込", new_stock_watchlist_path, class: btn_secondary_classes %>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full text-left text-sm">
        <thead>
          <tr class="border-b border-slate-200 text-slate-500">
            <th class="py-2 pr-3">銘柄</th>
            <th class="py-2 pr-3">業種</th>
            <th class="py-2 pr-3"></th>
          </tr>
        </thead>
        <tbody>
          <% @stocks.each do |stock| %>
            <% flags = @stock_flags.fetch(stock.id, { watching: false, holding: false, virtual_holding: false }) %>
            <tr class="border-b border-slate-100">
              <td class="py-2 pr-3">
                <div class="flex flex-wrap items-center gap-2">
                  <%= link_to stock_path(stock), class: "text-indigo-600 hover:underline" do %>
                    <span class="font-mono"><%= stock.code %></span>
                    <span class="font-medium"><%= stock.name %></span>
                  <% end %>
                  <% if flags[:holding] %>
                    <span class="rounded-full bg-indigo-50 px-2 py-0.5 text-xs font-medium text-indigo-700">エントリー中</span>
                  <% end %>
                  <% if flags[:virtual_holding] %>
                    <span class="rounded-full border border-amber-200 bg-amber-100 px-2 py-0.5 text-xs font-medium text-amber-800">仮想エントリー中</span>
                  <% end %>
                  <% if flags[:watching] && !flags[:holding] && !flags[:virtual_holding] %>
                    <span class="rounded-full bg-emerald-50 px-2 py-0.5 text-xs font-medium text-emerald-700">監視中</span>
                  <% end %>
                </div>
              </td>
              <td class="py-2 pr-3 text-slate-600"><%= stock.industry.name %></td>
              <td class="py-2 pr-3 whitespace-nowrap">
                <a href="<%= stock.tradingview_url %>" target="_blank" rel="noreferrer" class="inline-flex items-center rounded-lg bg-slate-900 px-2.5 py-1 text-xs font-medium text-white hover:bg-slate-800">チャート</a>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <% if @stocks.empty? %>
        <p class="py-6 text-center text-sm text-slate-500">エントリー中・監視中・仮想エントリー中の銘柄はありません</p>
      <% end %>
    </div>
  </section>
</div>
<%= render "layouts/stock_search" %>
