<div class="space-y-4">
  <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <h2 class="text-xl font-bold">
      <% if @expense.persisted? %>
        支出を編集
      <% else %>
        <%= recurring_type_label(@expense.expense_type) %>の支出を追加
      <% end %>
    </h2>
    <%= render "form" %>
  </section>
</div>
