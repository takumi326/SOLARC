<div class="space-y-4">
  <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <h2 class="text-xl font-bold">
      <% if @income.persisted? %>
        収入を編集
      <% else %>
        <%= recurring_type_label(@income.income_type) %>の収入を追加
      <% end %>
    </h2>
    <%= render "form" %>
  </section>
</div>
